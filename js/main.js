// Import libs
import $ from 'jquery';
import 'Modernizr';

(function () {
  // Run when DOM is ready
  $(function () {

  });

  // Run when DOM is changed
  Drupal.behaviors.oddbaby = {
    attach() {

    }
  };
})();
